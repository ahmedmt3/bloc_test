import 'package:flutter/material.dart';

class CharDetailsView extends StatelessWidget {
  const CharDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Char Details View"),
      ),
    );
  }
}
